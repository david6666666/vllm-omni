# Agent Instructions

## GitHub Account

- Use the GitHub account `david6666666` for all `gh`, git credential, push, and
  pull request operations.
- Do not use the GitHub account `lishunyang12` for this repository.
- Before any GitHub write operation, verify that `gh auth status` reports
  `david6666666` as the active account.
- Keep git author identity aligned with this account unless the user explicitly
  asks otherwise:
  - `user.name=david6666666`
  - `user.email=530634352@qq.com`
